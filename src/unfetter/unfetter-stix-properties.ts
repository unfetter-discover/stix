import { Dictionary } from '../common/dictionary';
import { MetaProps } from './meta-props';

export interface UnfetterStixProperties {
    metaProperties?: MetaProps;
    extendedProperties?: Dictionary;
}
