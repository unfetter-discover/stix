/**
 * @description used by the UI to create a single group of related questions
 */
export interface WizardQuestionGrouping {
    groupingValue: string;
    groupingName?: string;
    description?: string;
}
